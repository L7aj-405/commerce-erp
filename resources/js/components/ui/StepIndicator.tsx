export default function StepIndicator({ current }: { current: number }) { const steps = ['Fichier', 'Colonnes', 'Vérification', 'Import', 'Résultat']; return <ol className="mb-8 grid grid-cols-5 gap-2" aria-label="Étapes de l’import">{steps.map((step, index) => { const number = index + 1; const active = number === current; const done = number < current; return <li key={step} className="min-w-0"><div className={`h-1 rounded-full ${done || active ? 'bg-slate-950' : 'bg-slate-200'}`} /><div className={`mt-2 truncate text-xs ${active ? 'font-semibold text-slate-950' : 'text-slate-500'}`}>{number}. {step}</div></li>; })}</ol>; }
