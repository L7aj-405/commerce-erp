import { Link } from '@inertiajs/react';
type PageLink = { url: string | null; label: string; active: boolean };
export default function Pagination({ links }: { links: PageLink[] }) { return <nav aria-label="Pagination" className="mt-6 flex flex-wrap gap-1.5">{links.map((link, index) => link.url ? <Link key={index} href={link.url} preserveScroll preserveState className={`flex min-h-10 min-w-10 items-center justify-center rounded-lg border px-3 py-1.5 text-sm ${link.active ? 'border-slate-950 bg-slate-950 text-white' : 'border-slate-200 bg-white hover:bg-slate-50'}`}>{link.label.replace('&laquo;', '‹').replace('&raquo;', '›')}</Link> : null)}</nav>; }
