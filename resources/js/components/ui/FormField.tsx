import type { InputHTMLAttributes } from 'react';
export default function FormField({ label, error, ...props }: InputHTMLAttributes<HTMLInputElement> & { label: string; error?: string }) { return <label className="block"><span className="text-sm font-medium text-slate-800">{label}</span><input {...props} className={`mt-1 w-full rounded-lg border px-3 py-2 outline-none focus:ring-2 ${error ? 'border-red-400 focus:ring-red-100' : 'border-slate-300 focus:border-slate-500 focus:ring-slate-200'}`} />{error && <span className="mt-1 block text-sm text-red-600">{error}</span>}</label>; }
