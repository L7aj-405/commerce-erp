export default function SearchInput({ value, onChange, searching, placeholder = 'Rechercher…' }: { value: string; onChange: (value: string) => void; searching?: boolean; placeholder?: string }) {
    return <label className="relative block min-w-64 flex-1"><span className="sr-only">Rechercher</span><input type="search" value={value} onChange={event => onChange(event.target.value)} placeholder={placeholder} className="h-10 w-full rounded-lg border border-slate-300 bg-white px-3 pr-24 text-sm outline-none focus:border-slate-500 focus:ring-2 focus:ring-slate-200" />{searching && <span className="absolute right-3 top-2.5 text-xs text-slate-500">Recherche…</span>}</label>;
}
