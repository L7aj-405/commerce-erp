const labels: Record<string, string> = { active: 'Actif', inactive: 'Inactif', suspended: 'Suspendu', ready: 'Prêt', warning: 'Avertissement', skipped: 'Ignoré', error: 'Erreur', failed: 'Échec', imported: 'Importé', uploaded: 'Fichier analysé', previewed: 'Vérifié', processing: 'En cours', completed: 'Terminé', completed_with_errors: 'Terminé avec erreurs', paid: 'Payé', cancelled: 'Annulé', draft: 'Brouillon', confirmed: 'Confirmé' };
export default function StatusBadge({ status }: { status: string }) { const good = ['active', 'ready', 'imported', 'completed', 'paid'].includes(status); const bad = ['error', 'failed', 'inactive', 'cancelled', 'suspended'].includes(status); return <span className={`inline-flex rounded-full px-2.5 py-1 text-xs font-medium ${good ? 'bg-emerald-50 text-emerald-700' : bad ? 'bg-red-50 text-red-700' : 'bg-amber-50 text-amber-700'}`}>{labels[status] ?? status}</span>; }
